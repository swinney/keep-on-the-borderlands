M17c in progress — loop writing docs/installation.md (operator guide to install +
run the actual MUD: install → migrate → first-boot build_all population → evennia
start → connect telnet/web) and refreshing the stale README (pre-alpha → v1
acceptance-complete) before the M17c review gate. Docs-only; commands must be
accurate (operator review verifies them against the tree).

M17a (Django-free conftests, PR #24) and M17b (spell disruption, PR #25) merged.
v1 acceptance complete (M16, PR #23).
