; Minimal, empty pytest config for the kit's own tests. Passed explicitly via
; `pytest -c` in run.sh so the kit suite ignores whatever pytest configuration
; the surrounding project repo defines (testpaths, plugins, strict-config, a
; DJANGO_SETTINGS_MODULE, etc.) and stays self-contained.
[pytest]
